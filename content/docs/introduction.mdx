---
title: Introduction
description: The open-source infrastructure for production AI agents and knowledge bases.
---

import { Step, Steps } from 'fumadocs-ui/components/steps';
import { Callout } from 'fumadocs-ui/components/callout';
import { Card, Cards } from 'fumadocs-ui/components/card';
import { MessageSquare, Library, Construction, ShieldCheck, Cpu, Zap } from 'lucide-react';

# Introduction

> _"Build once, deploy everywhere."_

<div style={{textAlign: 'center'}}>
  <strong>OpenAgent is a single-binary AI agent platform — download, run, and your first agent is live in minutes.</strong><br />
  Orchestration, RAG, MCP, and tool-calling — all self-hosted, backed by your own data.
</div>

<Cards>
  <Card icon={<Zap className="text-blue-500" />} title="Install" description="Get OpenAgent running and send your first AI-powered message in under 5 minutes." href="/docs/install" />
  <Card icon={<Construction className="text-green-500" />} title="Build a Chat Assistant" description="Step-by-step guide to creating your first agent." href="/docs/build-a-chat-assistant" />
</Cards>

## What is OpenAgent?

OpenAgent is a **self-hosted AI agent platform** that connects your data, tools, and models into a production-ready system. Unlike simple chat interfaces, OpenAgent provides:

- **Managed RAG**: Turn PDFs and wikis into grounded context with automatic parsing and semantic search.
- **Model Orchestration**: Switch between 28+ providers with standardized abstractions for models, embeddings, and storage.
- **Tool Integration**: Extend agents with MCP servers and built-in automation.
- **Enterprise Security**: Role-based access control (RBAC), SSO integration, and comprehensive audit logs.

**Who is it for?** Developers and teams who want a private, self-hosted AI assistant that can reason over their own data — without relying on hosted APIs.

**What makes it different?**

- **Single Binary**: Ships as one executable — download and double-click to run. No installation wizard, no dependencies to manage.
- **Self-hosted**: Runs on your own infrastructure. Full control, no vendor lock-in.
- **Provider Agnostic**: Standardized abstraction for models, embeddings, and storage.
- **Scalable Workflows**: Connect agents to external tools, data, and automation through a consistent provider model.
- **Audit Ready**: Track every token, record every tool call, and monitor agent performance at scale.

<Cards>
  <Card icon={<MessageSquare className="text-purple-500" />} title="Model Orchestration" description="Seamlessly switch between 28+ providers. Configure fallback models for high availability." href="/docs/build-a-chat-assistant" />
  <Card icon={<Library className="text-orange-500" />} title="Managed RAG" description="Professional document indexing and retrieval. Turn PDFs and wikis into grounded context." href="/docs/add-a-knowledge-base" />
  <Card icon={<Cpu className="text-red-500" />} title="Tool Integration" description="Extend agents with MCP servers and built-in automation tools." href="/docs/tools-and-automation" />
  <Card icon={<ShieldCheck className="text-cyan-500" />} title="Operational Security" description="Role-based access control (RBAC), SSO integration, and comprehensive audit logs." href="/docs/admin/enterprise-sso" />
</Cards>

## Quick start

<Steps>
  <Step title="Deploy OpenAgent">
    The simplest way to run OpenAgent is to **download the binary** from the [Releases page](https://github.com/the-open-agent/openagent/releases) and double-click it. No installation required — it works natively on Windows, macOS, and Linux.

    **Windows** — download `openagent-windows-x86.exe` and run it directly. No WSL, no Docker needed.

    **macOS / Linux** — download the binary and make it executable:
    ```bash
    chmod +x openagent-linux-x86 && ./openagent-linux-x86
    ```

    Once running, navigate to **`http://localhost:14000`**.
  </Step>
  <Step title="Configure a Provider">
    Navigate to **Providers → Add Provider**.
    - **Category**: `Model`
    - **Type**: `DeepSeek` (Recommended for cost) or `OpenAI`
    - **API Key**: Enter your credentials.
    - **Model (Sub Type)**: Select a supported model ID.
  </Step>
  <Step title="Initialize an Agent (Store)">
    Navigate to **Stores → Add Store**.
    - **Name**: `primary-assistant`
    - **Model Provider**: Select the provider from Step 2.
    - **System Prompt**: _"You are a professional research assistant."_
  </Step>
  <Step title="Start Chatting">
    Open the **Chat** interface from the sidebar. Select your agent and send a test message.

    **Milestone Reached:** You now have a functional, self-hosted AI orchestration engine.
  </Step>
</Steps>

Need a deeper walkthrough? See [Build a Chat Assistant](/docs/build-a-chat-assistant).

## Start here

<Cards>
  <Card title="Install" description="Deploy OpenAgent and reach the First Message milestone." href="/docs/install" />
  <Card title="Chat Assistant" description="Create a chat assistant backed by any LLM provider." href="/docs/build-a-chat-assistant" />
  <Card title="Knowledge Base" description="Enable RAG so your assistant answers from your own documents." href="/docs/add-a-knowledge-base" />
  <Card title="Tools & Automation" description="Give your assistant the ability to browse, call APIs, or run automations." href="/docs/tools-and-automation" />
  <Card title="Admin & Enterprise" description="Manage users, organizations, and SSO integration." href="/docs/admin/enterprise-sso" />
</Cards>

<Callout type="info">
  **Note on Migration:** This documentation covers the new Agent-first architecture. Legacy cloud and multimedia modules from previous versions are now deprecated.
</Callout>
